CREATE TABLE [dbo].[ITEM] (
    [i_id]              INT          NOT NULL,
    [i_weight]          DECIMAL (18) NULL,
    [i_gp]              DECIMAL (18) NULL,
    PRIMARY KEY CLUSTERED ([i_id] ASC)
);

